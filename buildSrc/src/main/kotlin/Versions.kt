// Version constants for the Kotlin DSL dependencies
object Versions {
    // App level
    /**
     * Do not upgrade Gradle
     * due compatibility with IntelliJ IDEA
     * */
    const val gradleVersion = "4.1.0"
    const val kotlinVersion = "1.5.0"

    // HMS
    const val hmsCore = "1.4.1.300"

    // Test
    const val junit = "4.13.2"
    const val testCore = "1.3.0"
    const val extJunit = "1.1.2"
    const val espresso = "3.3.0"
    const val archCore = "2.1.0"
    const val coroutinesTest = "1.3.3"
    const val mockWebServerTest = "4.1.0"
    const val mockTest = "1.9.3"

    // Libs
    const val coreKtx = "1.3.2"
    const val appcompat = "1.2.0"
    const val legacyV4 = "1.0.0"

    // Android X
    const val activityKtx = "1.2.2"
    const val fragmentKtx = "1.3.2"
    // Navigation UI
    const val navigationUI = "2.3.4"

    const val lifecycleViewmodel = "2.2.0"
    const val lifecycleLivedata = "2.3.1"
    const val lifecycleExt = "2.2.0"

    const val workVersion = "2.4.0"

    // Material Design
    const val materialDesign = "1.3.0"
    // ConstraintLayout
    const val constraintLayout = "2.0.4"
    // RecyclerView
    const val recyclerView = "1.2.0"
    // ViewPager 2
    const val viewPager = "1.0.0"

    // Kotlin features
    const val coroutinesCore = "1.3.9"
    const val coroutinesAndroid = "1.4.1"

    // Koin
    const val koinVersion = "3.0.2"

    // Coil
    const val coil = "0.13.0"

    // Chrome Tabs
    const val chromeTabs = "28.0.0"

    // ThreeTeenABP
    const val threetenABP = "1.2.4"

    // Retrofit
    const val retrofitGson = "2.9.0"
    const val retrofit = "2.9.0"
    const val gson = "2.8.6"
    const val okhttp = "4.2.1"
    const val okhttpLoggingInterceptor = "4.7.2"
    const val retrofitCoroutines = "0.9.2"
    const val conscrypt = "2.4.0"

    // Ktor
    const val ktorCore = "1.6.0"
    const val ktorCIO = "1.6.0"

    // Room
    const val room = "2.3.0"

    // Firebase
    // Analytics
    const val fAnalytics = "18.0.3"
    // Crashlytics
    const val fCrashlytics = "17.4.1"
    // Dynamics Links
    const val fDynamicLink = "19.1.0"
    const val fMessaging = "20.2.4"
    const val fAuth = "19.4.0"
    const val fServicesAuth = "18.1.0"
    // In-App billing
    const val billing = "3.0.3"

    // Desugaring API
    const val desugaringApi = "1.1.5"

    // Yandex Mapkit
    const val yandexMapkit = "4.0.0-lite"
}